package com.example.sfeduhelper.classes

class InfoInstitute (name: String, desc: String) {
    constructor(): this(name = "ICTIS", desc = "just some text for now")

    var name = name
    var desc = desc
}